# HW13 – токенизация текста, инференс BERT и базовый fine-tuning для классификации

## 1. Кратко: что сделано

- Какой датасет использовался и почему.
- Какая BERT-подобная модель использовалась.
- Что было показано в части токенизации.
- Что показал инференс готовой модели.
- Какой результат дал fine-tuning.

```
Я исползовал датасет dair-ai/emotion/, как самый понятный и простой для работы.  
Для классификации текстов ипользовалась модель j-hartmann/emotion-english-distilroberta-base.  
В части токенизации я наглдядно показал, как текст превращается в набор токенов с добавлением padding.  
Инференс готовой модели показал хороший результат, но присутсвовало несоответствие классов, что делало модель не свосем подходящей.  
Fine-tuning позовлил переобучить модель именно под классы нешей задачи.  
```
## 2. Среда и воспроизводимость

- Python: `3.13.7`
- datasets: `4.8.4`
- transformers: `5.4.0` + `pip install transformers[torch]` (Using the `Trainer` with `PyTorch` requires `accelerate>=1.1.0`)
- torch: `2.10.0`
- Устройство (CPU/GPU): `CPU`
- Seed: `42`
- Как запустить: открыть `HW13.ipynb` и выполнить Run All.

## 3. Данные и постановка задачи

- Датасет: `emotion`
- Число классов: `6`
- Размер `train`: `16000`
- Размер `validation`: `2000`
- Размер `test`: `2000`
- Что именно классифицируется: `эмоциональный окрас предложений`
- Комментарий (2-4 предложения): что важно в данных с точки зрения длины текстов, числа классов, баланса классов и сложности задачи.
```
Датасет содержит набор небольших текстов, которые можно встретить в комментариях/на формуах/в чат румах/твитах.  
Всего в датасете 6 классов: 'sadness': 0, 'joy': 1, 'love': 2, 'anger': 3, 'fear': 4, 'surprise': 5.
В датасете абсолютное большинство занимают классы joy и sadness.
```

## 4. Токенизация и готовый инференс

### 4.1. Токенизация

- Какая модель токенизатора использовалась: `RobertaTokenizer`
- Какие special tokens используются: `<s>, </s>`
- Показан ли пример `padding` / `truncation`: `да`
- Что показал разбор токенизации на нескольких примерах: `Мы увидели, как токенизатор преобразует текст в набор токенов, который понимает машина.`

### 4.2. Инференс готовой pretrained модели

- Какая готовая модель использовалась: `j-hartmann/emotion-english-distilroberta-base`
- На каких примерах проверялся инференс: `На первых 10 предложениях из train`
- Насколько результаты готовой модели оказались разумными: `Модель показала неплохой результат по классификации классов.`
- Почему готовая модель подходит или не подходит под выбранную задачу: `Данная модель не совсем подходит под данную задачу, т.к. у модели и датасета немного отличаются классы`

## 5. Fine-tuning и оценка

Опишите коротко и по делу:

- Модель для fine-tuning: `j-hartmann/emotion-english-distilroberta-base`
- Как была организована токенизация датасета: `RobertaTokenizer` + `DataCollatorWithPadding`
- Максимальная длина (`max_length`): `максимальная длина формировалась по самому длинному предложению в батче`
- Batch size: `16`
- Максимальное число эпох: `4`
- Optimizer / learning rate: `ADAMW_TORCH_FUSED`;`lr` = `2e-5` 
- Критерий выбора лучшего варианта: `f1_macro`
- Какие метрики считались: `Accuracy`, `F1 Macro`, `F1 Weighted`

## 6. Результаты

Ссылки на файлы в репозитории:

- Примеры предсказаний: `./artifacts/sample_predictions.csv`
- Матрица ошибок: `./artifacts/confusion_matrix.png`

Короткая сводка (6-10 строк):

- Итоговая `test_accuracy`: `0.9315`
- Итоговая `test_f1_macro`: `0.8878`
- Что показал инференс готовой модели: `невозможность распознавать класс love`
- Что изменилось после fine-tuning: `Теперь модель может распознать love, но все равно испытвает небольшие трудности`
- Какие классы распознаются лучше всего:  `sadness`, `joy`, `anger`
- Какие классы модель путает чаще всего: `love/joy`, `fear/surprise`

## 7. Анализ

(8-15 предложений)

Нужно прокомментировать:

- что показал разбор токенизации;
- насколько полезным оказался инференс готовой модели до дообучения;
- почему fine-tuning улучшил или не улучшил результат;
- какие ошибки модели оказались наиболее показательными;
- какие ограничения у выбранной постановки и текущего эксперимента.
```
Разбор токенизации дал наглядно понять, как можно представить текст в понятном для машины виде.  
Инференс оказался довольно полезным, как базовая модель. Но у нее есть недостаток в виде невозможности предсказать класс 'love' (модель всегда предсказывала 'joy'). Это может быть критичным пунктом, если нам важно разграничивать класс 'love' от 'joy'.  
Fine-tuning как минимум позволила научить модель отличать класс 'love'.  
Но этот пункт все еще является самымы слабым у этой модели, и она часто путает классы 'love' и 'joy'. Но стоит отдать должное модели, т.к. эти классы очень смежны между собой.  
У данного эксперимента есть ограничение в виле узконаправленности модели четко под классы датасетов.  
```
## 8. Итоговый вывод

(3-7 предложений)

- Какую модель и конфиг вы бы взяли как базовый для такой задачи.
- Что главное вы поняли про токенизацию текста для трансформеров.
- Что главное вы поняли про различие между готовым инференсом и fine-tuning.
```
Как базовую я бы взял конфиг с fine-tunning модели, т.к. она специально натренирована под классы датасета.  
Я узнал, как правильно представлять текст в удобно для обучения форме.  
Инференс готовой модели редко идеально подходит под задачу, т.к. зачастую готовые модели обучается на немного других датасетах.  
Fine-tunning позволяет адаптировать уже сильную модель под свою задачу.
```

## 9. Приложение (опционально)

Если вы делали дополнительные сравнения:

- сравнение двух моделей;
- сравнение двух `max_length`;
- дополнительные графики; `training_curves.png`
- расширенный анализ ошибок.
